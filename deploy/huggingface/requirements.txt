# Slim dependency set for the demo Space (no chromadb / openai needed in demo
# mode). Keep in sync with the app's import needs.
streamlit==1.58.0
langgraph==1.1.10
langchain-core==1.4.1
pypdf==6.9.2
python-dotenv==1.0.1
pydantic==2.12.3
